# Used while building the frontend image in GitHub Actions.
# Store the real values in repo secret FRONTEND_BUILD_ENV_PRODUCTION (multiline).
# Deploy/promote workflows inject GITHUB_SHA + SENTRY_RELEASE from the image tag.
#
# Required for Sentry sourcemap upload (build fails in deploy if missing):
#   SENTRY_AUTH_TOKEN, SENTRY_ORG, SENTRY_PROJECT, VITE_SENTRY_DSN
# Optional:
#   SENTRY_URL (default https://de.sentry.io for EU)

VITE_API_URL=https://api.tuforums.com
VITE_HEALTH_URL=https://api.tuforums.com
VITE_DEV_API_URL=https://api.tuforums.com
VITE_STAGING_API_URL=https://api.staging.tuforums.com
VITE_PROD_API_URL=https://api.tuforums.com
VITE_OWN_URL=https://tuforums.com
VITE_OWN_DEV_URL=http://127.0.0.1:5173
VITE_OWN_STAGING_URL=https://staging.tuforums.com
VITE_OWN_PROD_URL=https://tuforums.com
VITE_CDN_URL=https://cdn.tuforums.com
VITE_DEV_CDN_URL=https://cdn.tuforums.com
VITE_PROD_CDN_URL=https://cdn.tuforums.com
VITE_APRIL_FOOLS=false
VITE_CUSTOM_PROFILE_BANNERS_ENABLED=true
VITE_RECAPTCHA_SITE_KEY=__REQUIRED__

# Runtime Sentry (baked into the browser bundle)
VITE_SENTRY_DSN=__REQUIRED__

# Build-time Sentry (sourcemaps / release) — NOT VITE_*; never ship token to the browser
SENTRY_AUTH_TOKEN=__REQUIRED__
SENTRY_ORG=the-universal-forums
SENTRY_PROJECT=javascript-react
SENTRY_URL=https://de.sentry.io
# SENTRY_RELEASE / GITHUB_SHA are set by the deploy workflow to the image git SHA
