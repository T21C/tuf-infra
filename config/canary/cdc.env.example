# CDC is disabled by default in compose.canary.yml. Enable only with an isolated schema.
CDC_HEALTH_PORT=13990
CDC_BIND_ADDRESS=127.0.0.1
CDC_SERVER_ID=999902
# Inherit production CDC_DB_USER via tuf-init-canary merge, or set a canary-only reader.

